#summary This page explain the WildcardPath concept
#labels ResourceBundle,Properties
#sidebar TableOfContents

*(Needs translation)*

= Introduction =
WildCardPath is a piece of text that try to describe an estructure of folder + file + locale (language & country) used by the plugin to discover asociated files.
This piece of text is like a simple regular expresion (simplified) that show to the plugin, how to search files into your project.

= Details =
The wildcard path have fixed tokens that can be used in distincts form to build the path to some file.

The common structure is:
*/{root}/{filename}.{lang}.{country}.{fileextension}*

 * Folder bars are '/'
 * {root} Represents the folder base to store the properties
 * {filename} Is the wildcard that represents the filename
 * {fileextension} Again is the representation of fileextension
 * {lang} Represents 2 letters lower case code of language [http://www.iso.org/iso/iso_catalogue/catalogue_tc/catalogue_detail.htm?csnumber=43380 ISO Language Codes]
 * {country} Represents 2 letters upper case country codes [http://www.iso.org/iso/english_country_names_and_code_elements ISO Country Codes]
(Under update)
[http://localizedproperties.googlecode.com/svn/wiki/images/wildcardpathEditor.png]

It has been extended wildcard functionality and we can describe optional attributes and an order to avoid it to try to match similar files.
Now you can enclose part of path into curly brackets and number to the end. Let's go to an example

{{{/{root}/{filename}*(.{lang})2*.{country}.{fileextension}}}}

== [http://java.sun.com/developer/technicalArticles/Intl/ResourceBundles/ Java] ==
  * El property por default se encuentra en `com.mipaquete.MiProperty.properties`
  * El property para es AR se encuentra en `com.mipaquete.MiProperty.es_AR.properties`
  * La i18n de en US se encuentra en `com.mipaquete.MiProperty.en_US.properties`

== Struts ==
_To Explain_

== [http://livedocs.adobe.com/flex/3/html/help.html?content=l10n_6.html Flex] ==
  * Generalmente la localizacion se encuentra en un directorio llamado locale al mismo nivel que eldirectorio de fuentes src

== WildcardPath Configuration Screen ==

[http://localizedproperties.googlecode.com/svn/wiki/images/localizedPreferences.png]